# mint-application-shell — Tasks

## Implementation

- [ ] 1.1 Plan this change once its spec work is settled

## Archive

- [ ] 2.1 On explicit author instruction, `pnpm spec:fold mint-application-shell` then `openspec archive --skip-specs -y mint-application-shell` at the tail of the PR that completes the implementation
