{
  "name": "@cyphid/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "pnpm -w run build:packages",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^25.9.0",
    "typescript": "*",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
