// spec: global-invariants/single-convex-deployment
// Empty until the capability changes that own the tables land — see AGENTS.md.
import { defineSchema } from "convex/server";

export default defineSchema({});
