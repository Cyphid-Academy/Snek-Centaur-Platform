# @cyphid/snek-platform-auth

The shared, zero-dependency credential vocabulary for the Team Snek Centaur Platform's `identity-and-authorization` capability — the game-token subject codec, the capability claim's structure and vocabulary, and audience naming — depended on by every runtime that mints or admits on the platform's credentials so minting and admission can never drift apart.
